{
  "scripts": {
    "start": "run-p server watch",
    "server": "http-server docs -c 1",
    "watch": "tsc -w",
    "build": "tsc"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.2.4"
  }
}
